
- Et TimeSheet bør opprettes automatisk når en Student lager en Group, eller når en Student legges til en Group
	* kan dette gjøres i GroupsController?
	* eller må dette skje fra app ved å kalle "ProjectJediDataSource.PostTimeSheet(...)" når "CreateGroup(....., Student s)" kalles i DataSource?

HUSK!: lage en BlancPage hvor data hentes fra DB, og som viser App-logo + ProgressRing/eller noe lignende
	- ProgressRing
	- Check Internet connection
		- Alert user if no internet to avoid app-crash...
			* "You have no Internet connection. The application will not function properly without a connection."

	- 1. Lage async methods for alle CRUD operasjoner 
	- 2. Når man oppretter bruker:
		- lagre passord til AppData/autorisere innlogging fra AppData
			- hvis bruker forblir pålogget, ikke autorisere(bypass log-in)
		- [legge til skole] - hvis ting ikke funker når man skal linke Stuenter til Grupper
			* schoolId kan brukes til å hente studenter som kan legges til i Group(se #3)
	- 3. Når man henter Studentder
		- bare hente de man er på gruppe sammen med
			* 

	- 4. Når man legger Student til Group, bruke PUT["kopierer" Grop, og legge til den nye Student i Group.Students]

I StudentTasks:
- getGroupAsync(groupId) - trenger jeg Groups på StudentTaskPage for å kunne legge en Task til riktig Group?
		
Design:
- Legge til Logo på DataLoadingPage.xaml
- Lage custom Style for bakgrunner, margin, padding, font, etc.

- bytte alle textressurser til StaticResource....